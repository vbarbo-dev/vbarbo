---
const preferredLanguage = Astro.preferredLocale?.toLowerCase();

const isPortuguese =
  preferredLanguage?.startsWith('pt');

const redirectTo = isPortuguese
  ? '/pt-br'
  : '/en';

return Astro.redirect(redirectTo);
---