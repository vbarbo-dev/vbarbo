---
import type { Card } from '../../../i18n/types';
import { ArrowUpRight } from 'lucide-astro';

interface Props { 
  cards: Card[] 
}

const { cards } = Astro.props as Props;
---

<div class="cards">
  {cards.map((card) => {
    const Icon = card.icon;

    return (
      <div class="card reveal-item" data-project={card.slug} data-preview={card.preview}>
        <div class="card-header">
          {card.logo ? (
            <div class="logo">
              <img src={card.logo} alt={`Logo ${card.title}`} title={`Logo ${card.title}`} loading="lazy" />
            </div>
          ) : Icon ? (
            <div class="icon">
              <Icon />
            </div>
          ) : null}
          <span class="card-title">{card.title}</span>
          <div class="external-arrow"><ArrowUpRight /></div>
        </div>

        <span class="card-desc">{card.desc}</span>

        <div class="card-impact">
          <span class="impact">{card.impact}</span>
          <span class="impact-desc">{card.impactDesc}</span>
        </div>

        <div class="card-tags">
          {card.tags.map(tag => {
            return(<div class="tag">{tag}</div>)
          })}
        </div>
      </div>
    );
  })}
</div>