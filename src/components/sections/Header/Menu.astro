---
import type { Menu } from '../../../i18n/types';

interface Props {
  menu: Menu[];
}

const { menu } = Astro.props as Props;
---

<ul class="menu">
  {menu.map((item) => {
    return(
      <li class="menu-item">
        <a href={item.link}>{item.title}</a>
      </li>
    )
  })}
</ul>