---
import { navigation } from "../../../data/navigation";
const { t } = Astro.props;
---

<ul class="menu">
  {navigation.map((item) => (
    <li class="menu-item"><a href={item.href} title={t.nav[item.key]}>{t.nav[item.key]}</a></li>
  ))}
</ul>